import {
  EnvironmentProviders,
  Provider
} from '@angular/core';

export const provide<%= className %> = (): Array<
  Provider | EnvironmentProviders
> => {
  const providers: Array<Provider | EnvironmentProviders> = [
  ];

  return providers;
};


