{
  "$schema": "https://json-schema.org/schema",
  "$id": "Package",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "enum": ["app"],
      "description": "",
      "default": "app",
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}
