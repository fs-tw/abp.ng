import { mapEnumToOptions } from '@abp/ng.core';

export const enum e<%= className %>PolicyNames {
  <%= className %> = '',
}
export const enum e<%= className %>Names {
  <%= className %> = '<%= resourceName %>::<%= className %>',
}

export enum eIdentityTwoFactorBehaviour {
  Optional = 0,
  Disabled = 1,
  Forced = 2,
}

export const identityTwoFactorBehaviourOptions = mapEnumToOptions(
  eIdentityTwoFactorBehaviour
);

